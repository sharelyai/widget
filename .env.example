# Backend API the widget talks to. Defaults to https://api.sharely.ai if unset.
VITE_API_DEFAULT_URL="https://your-api.example.com"

# Workspace identifier issued by your backend.
VITE_WORKSPACE_ID=""
