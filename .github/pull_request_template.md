## Summary

<!-- What does this PR change, and why? -->

## Checklist

- [ ] `pnpm test` passes
- [ ] `pnpm build` passes
- [ ] `pnpm lint` passes
- [ ] A changeset was added (`pnpm changeset`) if a package under `packages/` changed
- [ ] Docs / examples updated if behavior changed

## Notes for reviewers

<!-- Anything that needs extra attention, context, or follow-up. -->
